// src/common/interfaces/working-status.enum.ts
export enum WorkingStatus {
    PRESENT = 'present',
    ABSENT = 'absent',
    LATE = 'late',
    EARLY_DEPARTURE = 'early_departure',
    ON_LEAVE = 'on_leave',
    OFFICIAL_TRAVEL = 'official_travel',
    REMOTE_WORKING = 'remote_working',
  }